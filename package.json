{
  "name": "sokoni",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "backend",
    "admin"
  ],
  "scripts": {
    "build": "npm run build --workspace=admin",
    "start": "npm run start --workspace=backend"
  },
  "dependencies": {
    "inngest": "^3.48.1"
  }
}